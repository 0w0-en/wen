This folder should contain the webfont files for 'JF Open 粉圓' so the site can load the handwritten font locally.

Place webfont files here (example names used by the CSS):
- JFOpen-Pink.woff2
- JFOpen-Pink.woff

Where to get the font:
- 'JF Open 粉圓' is provided by Justfont and may require a license. Download the webfont files from your font vendor account or use a licensed package.

After placing the files, reload the Django dev server (or clear browser cache) to see the font applied.
